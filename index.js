fullName = (first, last) => {
  return `${first} ${last}`
}

alert(fullName('Boli', 'Mon'))

console.log('');


// DOM --> Document Object Model

const user = {
  name: 'Ashley',
  email: 'ash@gmail.com',
  favs: [
    'Chipotle',
    'Chic fil a'
  ]
}

